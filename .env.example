# GitHub Repository Configuration
# Your GitHub repository in format: username/repository
REPO=wesleykirkland/docker-runner

# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required scope: repo (for repository-specific runners)
ACCESS_TOKEN=your_github_personal_access_token_here

# Optional: Custom labels for selective task execution
# Comma-separated list of labels to add to the runner
# Example: RUNNER_LABELS=docker,linux,deploy,production
# Use in workflows with: runs-on: [self-hosted, docker, deploy]
RUNNER_LABELS=

# Optional: Custom runner name
# If not set, GitHub will auto-generate a name
# Example: RUNNER_NAME=my-production-runner
RUNNER_NAME=

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Edit .env and replace the values above
# 3. Never commit .env to version control
# 4. Use with docker compose: docker compose --env-file .env up -d

